{
  "name": "@aethertable/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Componentes React genericos do design system (DOC-041). Consumidos pelo frontend; nao usados dentro do Canvas.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.7",
    "react": "^19.0.0",
    "typescript": "^5.7.3"
  }
}
